# Python依赖
akshare>=1.12.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dateutil>=2.8.2

# TuShare（需申请Token）
tushare>=1.4.0

# BaoStock（免费备用数据源）
baostock>=1.0.0

# TickFlow（末级保底数据源，可选）
# tickflow>=1.0.0

# 定时任务（可选，如果不用内置调度器）
# APScheduler>=3.10.0

# 日志增强（可选）
# colorlog>=6.7.0
